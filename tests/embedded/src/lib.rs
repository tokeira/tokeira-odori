//! Integration harness for the Odori primitives against embedded tokeira.
//!
//! All substance lives in `tests/embedded.rs`; run with
//! `cargo test --manifest-path tests/embedded/Cargo.toml` from the repo
//! root. See this package's `Cargo.toml` header for why it is
//! workspace-excluded.
